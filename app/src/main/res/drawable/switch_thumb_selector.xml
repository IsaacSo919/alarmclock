<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/switch_thumb_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color when switch is checked (ON) -->
    <item android:color="@color/blue" android:state_checked="true" />
    <!-- Default color when switch is not checked (OFF) -->
    <item android:color="@color/light_blue" />
</selector>
